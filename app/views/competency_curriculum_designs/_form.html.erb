<%= form_with(model: competency_curriculum_design, class: "contents") do |form| %>
  <% if competency_curriculum_design.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg mt-3">
      <h2><%= pluralize(competency_curriculum_design.errors.count, "error") %> prohibited this competency_curriculum_design from being saved:</h2>

      <ul>
        <% competency_curriculum_design.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5">
    <%= form.label :numero_competencia %>
    <%= form.number_field :numero_competencia, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full" %>
  </div>

  <div class="my-5">
    <%= form.label :codigo_diseño_curricural %>
    <%= form.text_field :codigo_diseño_curricural, class: "block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full" %>
  </div>

  <div class="inline">
    <%= form.submit class: "rounded-lg py-3 px-5 bg-blue-600 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
